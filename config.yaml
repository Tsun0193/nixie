fields:
  - receipt_id
  - document_date
  - export_date
  - fg_code
  - unit
  - quantity_carton
  - quantity_pcs
  - receiving_warehouse
  - address
  - ar_code

PROMPT_SYSTEM: |
  Bạn là công cụ phục hồi văn bản từ OCR.
  Đầu vào là MỘT chuỗi văn bản duy nhất từ OCR.
  - Ghép/giữ nguyên thứ tự ký tự như trong đầu vào.
  - Không được bỏ sót, không thêm ký tự mới; chỉ sửa lỗi OCR hiển nhiên (đặc biệt là dấu tiếng Việt).
  - Giữ nguyên chữ số, ký hiệu, dấu câu, chữ hoa/thường.

  Văn bản OCR:
  {text}

  Chỉ trả về CHÍNH XÁC văn bản đã phục hồi, không kèm giải thích.
